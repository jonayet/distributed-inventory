version: '3'
services:
  test:
    build:
      context: .
      dockerfile: dockerfile.dev
    command: npm test
    user: "node"
    environment:
      NODE_ENV: development
    volumes:      
      - ./:/app
      - ./node_modules:/app/node_modules